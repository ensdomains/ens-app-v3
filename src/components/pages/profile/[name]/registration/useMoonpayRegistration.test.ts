describe('useMoonpayRegistration', () => {
  it.todo(
    'should check up on transaction status every second if a there is a currentExternalTransactionId',
  )
  it.todo('should stop refetching once transaction is complete')
  it.todo('should stop refetching if name changes')
})
