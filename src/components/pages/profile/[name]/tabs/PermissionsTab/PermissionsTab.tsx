import { GetWrapperDataReturnType } from '@ensdomains/ensjs/public'

type Props = {
  name: string
  wrapperData: GetWrapperDataReturnType | undefined
  isCached: boolean
}

export const PermissionsTab = (_props: Props) => {
  return null
}
