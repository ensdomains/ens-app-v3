diff --git a/dist/functions/public/getOwner.js b/dist/functions/public/getOwner.js
index 6c23cac5dbf0f99bebac7cc7f6c2fb0a2ad59d6e..a30d0268f13ecd362491f03ae79c4c476efce17f 100644
--- a/dist/functions/public/getOwner.js
+++ b/dist/functions/public/getOwner.js
@@ -62,7 +62,8 @@ const decode = async (client, data, { name, contract }) => {
         contract: 'ensNameWrapper',
     });
     // check for only .eth names
-    if (labels[labels.length - 1] === 'eth') {
+    const tld = labels[labels.length - 1];
+    if (tld === 'eth' || tld === 'testing' || tld === 'simplex') {
         // if the owner on the registrar is the namewrapper, then the namewrapper owner is the owner
         // there is no "registrant" for wrapped names
         if (registrarOwner === nameWrapperAddress) {
diff --git a/dist/utils/checkSafeUniversalResolverData.js b/dist/utils/checkSafeUniversalResolverData.js
index 0194373eec98aa75970a9bef609a3a77d1c10f6a..d1aed41c4f95628a758093036d4c4966f33b86f3 100644
--- a/dist/utils/checkSafeUniversalResolverData.js
+++ b/dist/utils/checkSafeUniversalResolverData.js
@@ -23,8 +23,10 @@ export const checkSafeUniversalResolverData = (data, { strict, abi, args, functi
                 catch (_error) { }
             }
         }
-        if (data instanceof CallExecutionError)
+        if (data instanceof CallExecutionError) {
+            if (!strict) return false;
             throw data;
+        }
         throw getContractError(data, {
             abi,
             args: typeof args === 'function' ? args() : args,
diff --git a/dist/utils/generateFunction.js b/dist/utils/generateFunction.js
index ddc2285417b7746d90fcc6f2fbf0595621bdeaa0..d62564fe8134afa99fb443adb0b920db34cfe2d3 100644
--- a/dist/utils/generateFunction.js
+++ b/dist/utils/generateFunction.js
@@ -6,9 +6,7 @@ export const generateFunction = ({ encode, decode, }) => {
         const result = await call(client, encodedData)
             .then((ret) => ret.data)
             .catch((e) => {
-            if (!(e instanceof BaseError))
-                throw e;
-            return e;
+            return e instanceof BaseError ? e : new BaseError(e?.message || 'Unknown error');
         });
         if (passthrough)
             return decode(client, result, passthrough, ...args);
diff --git a/dist/utils/getNameType.js b/dist/utils/getNameType.js
index 76b725c3f43ad25c66ba5f2d1af3c0b363682c3b..3b1cab8d413817f9cc4b8b0b0fbe0ce2721a7f28 100644
--- a/dist/utils/getNameType.js
+++ b/dist/utils/getNameType.js
@@ -1,6 +1,7 @@
 export const getNameType = (name) => {
     const labels = name.split('.');
-    const isDotEth = labels[labels.length - 1] === 'eth';
+    const tld = labels[labels.length - 1];
+    const isDotEth = tld === 'eth' || tld === 'testing' || tld === 'simplex';
     if (labels.length === 0)
         return 'root';
     if (labels.length === 1) {
diff --git a/dist/utils/validation.js b/dist/utils/validation.js
index 7b2b2164b9aa9e4363f1449883af670bd33efea1..567ab70703086ada36e3abe5a21c499d0af85dcb 100644
--- a/dist/utils/validation.js
+++ b/dist/utils/validation.js
@@ -31,7 +31,7 @@ export const parseInput = (input) => {
     const normalisedName = isValid ? nameReference : undefined;
     const labels = nameReference.split('.');
     const tld = labels[labels.length - 1];
-    const isETH = tld === 'eth';
+    const isETH = tld === 'eth' || tld === 'testing' || tld === 'simplex';
     const labelDataArray = split(nameReference);
     const isShort = (labelDataArray[0].output?.length || 0) < MINIMUM_DOT_ETH_CHARS;
     if (labels.length === 1) {
@@ -56,5 +56,5 @@ export const parseInput = (input) => {
         labelDataArray,
     };
 };
-export const checkIsDotEth = (labels) => labels.length === 2 && labels[1] === 'eth';
+export const checkIsDotEth = (labels) => labels.length === 2 && (labels[1] === 'eth' || labels[1] === 'testing' || labels[1] === 'simplex');
 //# sourceMappingURL=validation.js.map
diff --git a/src/functions/public/getOwner.ts b/src/functions/public/getOwner.ts
index b3b572f4dd64e411143d85fc113d755d1f5788c8..1ce1cacf51ebe2f45780711e46840d77a25a0168 100644
--- a/src/functions/public/getOwner.ts
+++ b/src/functions/public/getOwner.ts
@@ -150,7 +150,8 @@ const decode = async <TContract extends OwnerContract | undefined = undefined>(
   })
 
   // check for only .eth names
-  if (labels[labels.length - 1] === 'eth') {
+  const tld = labels[labels.length - 1]
+  if (tld === 'eth' || tld === 'testing' || tld === 'simplex') {
     // if the owner on the registrar is the namewrapper, then the namewrapper owner is the owner
     // there is no "registrant" for wrapped names
     if (registrarOwner === nameWrapperAddress) {
diff --git a/src/utils/checkSafeUniversalResolverData.ts b/src/utils/checkSafeUniversalResolverData.ts
index f2a82ac5d28741ea2077c30e6f8be1950bf5e90c..002c938ecc28c0d26da6f20b08d42d9491f4a754 100644
--- a/src/utils/checkSafeUniversalResolverData.ts
+++ b/src/utils/checkSafeUniversalResolverData.ts
@@ -49,7 +49,10 @@ export const checkSafeUniversalResolverData = (
         } catch (_error) {}
       }
     }
-    if (data instanceof CallExecutionError) throw data
+    if (data instanceof CallExecutionError) {
+      if (!strict) return false
+      throw data
+    }
     throw getContractError(data, {
       abi,
       args: typeof args === 'function' ? args() : args,
diff --git a/src/utils/generateFunction.ts b/src/utils/generateFunction.ts
index aa70184efef3c52b3a77aec159daa857fe7e1e0d..bdeeba35e22b9937e3424f41c08c46404fdfca4c 100644
--- a/src/utils/generateFunction.ts
+++ b/src/utils/generateFunction.ts
@@ -72,8 +72,7 @@ export const generateFunction = <
     const result = await call(client, encodedData)
       .then((ret) => ret.data)
       .catch((e) => {
-        if (!(e instanceof BaseError)) throw e
-        return e
+        return e instanceof BaseError ? e : new BaseError(e?.message || 'Unknown error')
       })
     if (passthrough) return decode(client, result, passthrough, ...args)
     return decode(client, result, ...args)
diff --git a/src/utils/getNameType.ts b/src/utils/getNameType.ts
index ae761486e270c8f899d6ea1e62731a5116452638..833261c8ebcaa4258561c3946cb0d4364af8f80a 100644
--- a/src/utils/getNameType.ts
+++ b/src/utils/getNameType.ts
@@ -2,7 +2,8 @@ import type { NameType } from '../types.js'
 
 export const getNameType = (name: string): NameType => {
   const labels = name.split('.')
-  const isDotEth = labels[labels.length - 1] === 'eth'
+  const tld = labels[labels.length - 1]
+  const isDotEth = tld === 'eth' || tld === 'testing' || tld === 'simplex'
 
   if (labels.length === 0) return 'root'
   if (labels.length === 1) {
diff --git a/src/utils/validation.ts b/src/utils/validation.ts
index e1e75d900afee29c8e18d78141234f2e512a9abf..4937757f25695fc8721780cc9ea5b26231cbf4fd 100644
--- a/src/utils/validation.ts
+++ b/src/utils/validation.ts
@@ -46,7 +46,7 @@ export const parseInput = (input: string): ParsedInputResult => {

   const labels = nameReference.split('.')
   const tld = labels[labels.length - 1]
-  const isETH = tld === 'eth'
+  const isETH = tld === 'eth' || tld === 'testing' || tld === 'simplex'
   const labelDataArray = split(nameReference)
   const isShort =
     (labelDataArray[0].output?.length || 0) < MINIMUM_DOT_ETH_CHARS
@@ -75,5 +75,5 @@ export const parseInput = (input: string): ParsedInputResult => {
   }
 }

-export const checkIsDotEth = (labels: string[]) =>
-  labels.length === 2 && labels[1] === 'eth'
+export const checkIsDotEth = (labels: string[]): boolean =>
+  labels.length === 2 && (labels[1] === 'eth' || labels[1] === 'testing' || labels[1] === 'simplex')
